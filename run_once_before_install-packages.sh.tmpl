#!/bin/bash
# macOS only
{{ if ne .chezmoi.os "darwin" }}exit 0{{ end }}

set -euo pipefail

echo "==> Installing Homebrew packages..."
brew bundle --file="{{ .chezmoi.sourceDir }}/os/darwin/Brewfile" --no-lock
echo "==> Done."
