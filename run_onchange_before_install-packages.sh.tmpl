#!/bin/bash
# macOS only
{{ if ne .chezmoi.os "darwin" }}exit 0{{ end }}
# Brewfile hash: {{ include "os/darwin/Brewfile" | sha256sum }}

set -euo pipefail

echo "==> Installing Homebrew packages..."
brew bundle --file="{{ .chezmoi.sourceDir }}/os/darwin/Brewfile"
echo "==> Done."
